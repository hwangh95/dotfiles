- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    #~/.dotfiles: ''
    ~/.gitconfig: 
    ~/.zshenv: 
    ~/.zshrc: 

- shell:
  - [git submodule update --init --recursive, Installing submodules]


